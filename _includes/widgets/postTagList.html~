{% comment %}
	Inline tag list for pages
	
	Author: Sebastian Teumert (<http://www.teumert.net>, <http://www.github.com/NetzwergX>)
	Licensed under the MIT license (see LICENSE file).
	
	This tag list can be used to display the tags with which this page was tagged.
	
	For more Jekyll widgets, see <http://www.github.com/NetzwergX/jekyll-template-toolkit>.
{% endcomment %}
{% if page.tags.size > 0 %}
  <div class="inlinetags">
    tags:
    {% for tag in page.tags %}
      <a href="/tags.html#{{tag | cgi_encode}}">{{ tag }}</a>
      {% unless forloop.last %}<span class="tag-separator">|</span>{% endunless %}
    {% endfor %}
  </div>	
{% endif %}		
